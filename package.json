{
  "name": "wridachic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "dev": "node build.mjs --watch --serve"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
